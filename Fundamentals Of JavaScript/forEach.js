/*forEach()
  array iterating using forEach function()
  1st parameter: item
  2nd parameter: index
  3rd parameter: whole array

  use min one parameter otherwise max three parameter
  used function as expression
*/

/*
let namesFd = ["P", "S", "V", "W", "X", "K"];

namesFd.forEach(function(v,i){
console.log(`index: ${i} & value: ${v}`);
});
*/

//or for better readability
// let namesFd = ["P", "S", "V", "W", "X", "K"];

// let keep = function(v,i){
//     console.log(`index: ${i} & value: ${v}`);
// };

// namesFd.forEach(keep);